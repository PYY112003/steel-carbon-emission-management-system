<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.steel.carbon.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.steel.carbon.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="dept_id" property="deptId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="dept_name" property="deptName"/>
    </resultMap>

    <insert id="insert" parameterType="com.steel.carbon.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, email, phone, dept_id, status)
        VALUES (#{username}, #{password}, #{realName}, #{email}, #{phone}, #{deptId}, #{status})
    </insert>

    <update id="update" parameterType="com.steel.carbon.entity.SysUser">
        UPDATE sys_user
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT u.*, d.dept_name
        FROM sys_user u
        LEFT JOIN dept_info d ON u.dept_id = d.id
        WHERE u.id = #{id}
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT u.*, d.dept_name
        FROM sys_user u
        LEFT JOIN dept_info d ON u.dept_id = d.id
        WHERE u.username = #{username}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT u.*, d.dept_name
        FROM sys_user u
        LEFT JOIN dept_info d ON u.dept_id = d.id
        ORDER BY u.create_time DESC
    </select>

    <select id="selectByDeptId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT u.*, d.dept_name
        FROM sys_user u
        LEFT JOIN dept_info d ON u.dept_id = d.id
        WHERE u.dept_id = #{deptId}
        ORDER BY u.create_time DESC
    </select>

</mapper>
